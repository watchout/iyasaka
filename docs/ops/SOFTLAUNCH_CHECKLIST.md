# IYASAKA ソフトローンチチェックリスト

**最終更新**: 2026年1月14日  
**目的**: 本番公開前の最終確認

---

## 📋 チェックリスト

### 1. 環境変数 ✅

| 項目 | 確認方法 | 状態 |
|------|----------|------|
| `SUPABASE_URL` 設定済み | `.env` 確認 | ☐ |
| `SUPABASE_SERVICE_KEY` 設定済み | `.env` 確認 | ☐ |
| `SLACK_LEAD_WEBHOOK` 設定済み（推奨） | `.env` 確認 | ☐ |
| `NUXT_PUBLIC_SITE_URL` 設定済み | `.env` 確認 | ☐ |

### 2. データベース ✅

| 項目 | 確認方法 | 状態 |
|------|----------|------|
| `leads` テーブル作成済み | Supabase Dashboard | ☐ |
| インデックス作成済み | Supabase Dashboard | ☐ |

**確認コマンド**:
```bash
curl -s http://localhost:4100/iyasaka/api/health/supabase
# 期待: {"ok":true}
```

### 3. API動作確認 ✅

| 項目 | 確認方法 | 状態 |
|------|----------|------|
| `/api/leads` POST正常 | curlテスト | ☐ |
| Supabaseにデータ保存 | Dashboard確認 | ☐ |
| Slack通知送信 | チャンネル確認 | ☐ |

**確認コマンド**:
```bash
curl -X POST http://localhost:4100/iyasaka/api/leads \
  -H "Content-Type: application/json" \
  -d '{"name":"ソフトローンチテスト","email":"test@example.com","employees":"1-10","interestedProducts":["mieru-plus"],"privacyAgreed":true,"source":"softlaunch-test","primaryProduct":"mieru-plus"}'
```

### 4. ページ表示確認 ✅

| ページ | URL | 状態 |
|--------|-----|------|
| トップ | `/iyasaka/` | ☐ |
| 診断 | `/iyasaka/diagnosis` | ☐ |
| 製品詳細 | `/iyasaka/products/mieru-plus` | ☐ |
| サンクス | `/iyasaka/contact/thanks` | ☐ |
| プライバシー | `/iyasaka/legal/privacy` | ☐ |
| 利用規約 | `/iyasaka/legal/terms` | ☐ |
| 外部送信 | `/iyasaka/legal/external-transmission` | ☐ |

### 5. フォーム動作確認 ✅

| 項目 | 確認方法 | 状態 |
|------|----------|------|
| お問い合わせフォーム送信 | 実際に送信 | ☐ |
| サンクスページ遷移 | 送信後確認 | ☐ |
| URLパラメータプリフィル | `?product=mieru-plus` | ☐ |
| バリデーション動作 | 空送信テスト | ☐ |

### 6. SEO/メタデータ ✅

| 項目 | 確認方法 | 状態 |
|------|----------|------|
| `sitemap.xml` 生成 | `/iyasaka/sitemap.xml` | ☐ |
| `robots.txt` 設定 | `/iyasaka/robots.txt` | ☐ |
| OGP画像設定 | SNSシェアプレビュー | ☐ |
| canonical URL正確 | ソース確認 | ☐ |

**確認コマンド**:
```bash
curl -s http://localhost:4100/iyasaka/sitemap.xml | head -20
curl -s http://localhost:4100/iyasaka/robots.txt
```

### 7. Plausible Analytics ✅

| 項目 | 確認方法 | 状態 |
|------|----------|------|
| トラッキングスクリプト設置 | ソース確認 | ☐ |
| Goals設定（4種） | Plausible Dashboard | ☐ |
| リアルタイム計測確認 | Dashboard確認 | ☐ |

**必要なGoals**:
1. `form_submit` - フォーム送信
2. `diagnosis_start` - 診断開始
3. `diagnosis_complete` - 診断完了
4. `download_start` - 資料DL開始

### 8. パフォーマンス ✅

| 項目 | 目標 | 確認方法 | 状態 |
|------|------|----------|------|
| Lighthouse Performance | 90+ | Chrome DevTools | ☐ |
| Lighthouse Accessibility | 90+ | Chrome DevTools | ☐ |
| Lighthouse Best Practices | 90+ | Chrome DevTools | ☐ |
| Lighthouse SEO | 90+ | Chrome DevTools | ☐ |

### 9. セキュリティ ✅

| 項目 | 確認方法 | 状態 |
|------|----------|------|
| HTTPS有効 | URLバー確認 | ☐ |
| サンクスページnoindex | ソース確認 | ☐ |
| 環境変数非公開 | クライアント確認 | ☐ |

### 10. 法務ページ ✅

| 項目 | 確認方法 | 状態 |
|------|----------|------|
| プライバシーポリシー内容正確 | 法務確認 | ☐ |
| 利用規約内容正確 | 法務確認 | ☐ |
| 外部送信規律内容正確 | 法務確認 | ☐ |
| フッターリンク動作 | クリック確認 | ☐ |

---

## 🚀 ローンチ手順

### Phase 1: 最終確認（当日朝）

```bash
# 1. サーバー状態確認
curl -s http://localhost:4100/iyasaka/

# 2. API確認
curl -s http://localhost:4100/iyasaka/api/health/supabase

# 3. テストリード送信
curl -X POST http://localhost:4100/iyasaka/api/leads \
  -H "Content-Type: application/json" \
  -d '{"name":"最終テスト","email":"final@test.com","employees":"1-10","interestedProducts":["mieru-plus"],"privacyAgreed":true,"source":"final-check"}'
```

### Phase 2: 公開

1. DNS設定（必要な場合）
2. Vercelへデプロイ（または本番サーバー設定）
3. 本番URLでの動作確認
4. Slack/メール通知テスト

### Phase 3: 監視開始

1. Plausible Analyticsでリアルタイム確認
2. Slackチャンネル監視
3. エラーログ監視

---

## 📊 初週KPI目標

| 指標 | 目標 |
|------|------|
| ページビュー | 計測開始 |
| 診断開始率 | 8%+ |
| フォーム送信 | 1件+ |
| 有効リード | 1件+ |

---

## 🚨 ロールバック手順

問題発生時:

1. **即時対応**: メンテナンスページ表示
2. **原因特定**: ログ確認
3. **修正**: ホットフィックス適用
4. **再デプロイ**: 動作確認後公開

---

**前のステップ**: [本番ENV設定ガイド](./PRODUCTION_ENV_GUIDE.md)
